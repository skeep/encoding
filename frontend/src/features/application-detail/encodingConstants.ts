/** Fields with composite score below this are listed in the low-confidence summary. */
export const LOW_CONFIDENCE_THRESHOLD = 0.85;

/** Sentinel `selectedTargetId` for the left-nav “Low confidence fields” aggregate view. */
export const LOW_CONFIDENCE_NAV_TARGET_ID = "__encoding_low_confidence__";
