# embedding-backend

FastAPI scoring service for the encoding project.

## Backend quick start

Run from this folder:

```bash
uvicorn app.api.server:app --reload --port 8000
```

Or run with Docker Compose:

```bash
docker compose up --build
```

## Notes

- Backend developer documentation lives in `docs/DEVELOPER_GUIDE.md`.